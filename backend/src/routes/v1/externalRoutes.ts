import { Router } from 'express';

const router = Router();

/**
 * @summary External (public) routes configuration
 * @remarks Routes for public access without authentication
 * @remarks Feature-specific routes will be added here
 */

export default router;
