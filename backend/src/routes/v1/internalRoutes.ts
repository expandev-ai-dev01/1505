import { Router } from 'express';

const router = Router();

/**
 * @summary Internal (authenticated) routes configuration
 * @remarks Routes requiring authentication
 * @remarks Feature-specific routes will be added here
 */

export default router;
